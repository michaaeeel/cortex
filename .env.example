# Django
# REQUIRED: generate with: python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=
DJANGO_SETTINGS_MODULE=cortex.settings.dev
DATABASE_URL=postgres://cortex:cortex@db:5432/cortex
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
POSTGRES_DB=cortex
POSTGRES_USER=cortex
# REQUIRED: set a strong password before use
POSTGRES_PASSWORD=

# Redis
REDIS_URL=redis://redis:6379/0

# ML Services
ML_SERVICE_URL=http://ml-services:8001
ML_ANTHROPIC_API_KEY=
# Shared secret between Django backend and ML service (optional, but recommended)
ML_INTERNAL_API_KEY=

# Frontend
VITE_API_URL=http://localhost:8000
